parent_config_to_override: "base_later.yaml"
run_name: "later"

# Trajectory generation settings
iterations: 10

# Training settings
learning_rate: 2.5e-5

# Influence model settings
veto_prompt_type: "five_point"

# Formatting penalty settings
formatting_penalty_scale_factor: 0.1

env_class: "later-cot"
iterations: 2
n_subenvs_to_sample_per_env: 5
num_envs_per_device: 2
n_trajs_to_sample_per_subenv: 4
